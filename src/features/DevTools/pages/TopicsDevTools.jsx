import TopicsBoard from '../../../components/TopicsBoard'
import { devToolsSections } from '../api/devToolsData'

function TopicsDevTools() {
  return <TopicsBoard sections={devToolsSections} />
}

export default TopicsDevTools
